# Використовуємо за базовий образ nginx
FROM nginx

# Створюємо каталог для файлів веб-сайту
WORKDIR /usr/share/nginx/html

# Копіюємо файли вашого веб-сайту у контейнер
COPY . .

# Вказуємо, на якому порту буде працювати наш веб-сайт
EXPOSE 80
